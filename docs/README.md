# Yaskawa Strategy Desk Code Scaffold

This zip contains the initial project scaffold for Sprint 1.

## Included
- FastAPI backend skeleton
- PostgreSQL configuration placeholders
- Initial SQLAlchemy models for `strategy_versions` and `cases`
- Next.js frontend shell

## Next Steps
1. Create the PostgreSQL database `yaskawa_strategy_desk`
2. Copy `.env.example` to `.env` and update values if needed
3. Install backend dependencies
4. Start FastAPI and verify `/health`
5. Initialize frontend dependencies and run the shell
